# Auth Web Tips
- Keep login and callback routes narrowly scoped.
- Keep principal token transport configuration consistent across environments.
- Treat codec changes as compatibility-sensitive.
- Use decorators for access control; avoid ad hoc checks in handlers.
