import { <%= model.className %>ModelConfig, <%= model.className %>ModelSource } from '@travetto/model-<%= model.packageName %>';
import { ModelSource } from '@travetto/model';
import { InjectableFactory } from '@travetto/di';

class Config {
  @InjectableFactory()
  static getSource(config: <%= model.className %>ModelConfig): ModelSource {
    return new <%= model.className %>ModelSource(config);
  }
}