import { <%= rest.className %>RestApp } from '@travetto/rest-<%= rest.packageName %>';
import { RestApp } from '@travetto/rest';
import { InjectableFactory } from '@travetto/di';

class RestConfig {
  @InjectableFactory()
  static getApp(): RestApp {
    return new <%= rest.className %>RestApp();
  }
}