{
  "author": {
    "email": "{{{author.email}}}",
    "name": "{{{author.name}}}"
  },
  "dependencies": {
    // {{#frameworkDependencies}}
    "{{{.}}}": "^{{{frameworkVersion}}}",
    // {{/frameworkDependencies}}
    "@travetto/cli": "^{{{frameworkVersion}}}"
  },
  "description": "A travetto based project",
  "keywords": [
    // {{#modules}}
    "{{{.}}}",
    // {{/modules}}
    "travetto"
  ],
  "license": "MIT",
  "scripts": {
    "start": "npx travetto run rest",
    "test": "npx travetto test"
  },
  "files": [
    "src",
    "resources"
  ],
  "name": "{{{app.name}}}",
  "version": "1.0.0"
}