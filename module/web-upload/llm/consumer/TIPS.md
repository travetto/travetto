# Web Upload Tips
- Always validate content type and max size.
- Use file maps when clients can submit multiple files.
- Keep storage handoff explicit and test failure paths.
- Avoid buffering large files in memory unless required.
