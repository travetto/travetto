/** @jsxImportSource @travetto/doc */
import { d, c } from '@travetto/doc';
import { WebApplication } from '@travetto/web';

export const text = <>
  <c.StdHeader />
  The module is an {d.library('Node')} provider for the {d.mod('Web')} module.  This module provides an implementation of {WebApplication} for automatic injection in the default Web server.
</>;
