export class Messages {
  static EMAIL = '{PATH} is not a valid email address';
  static TELEPHONE = '{PATH} is not a valid phone number';
  static URL = '{PATH} is not a valid url';
  static SIMPLE_NAME = '{PATH} is not a proper name';
  static POSTAL_CODE = '{PATH} is not a valid postal code';
  static REQUIRED = '{PATH} is required';
  static MINLENGTH = '{PATH} is not long enough ({MIN})';
  static MAXLENGTH = '{PATH} is too long ({MAX})';
}
