# Web Connect Tips
- Use bridge adapter only where third-party middleware demands it.
- Keep mapping code from middleware payloads to framework contracts explicit.
- Validate middleware behavior under async errors and callback failures.
- Avoid coupling application handlers directly to connect middleware APIs.
