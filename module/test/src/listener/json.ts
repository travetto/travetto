import { AllSuitesResult, TestResult, SuiteResult } from '../model';
import { Listener } from '../service';
import Collector from './collector';

export default class JSONListener extends Collector {
  complete() {
  }

}