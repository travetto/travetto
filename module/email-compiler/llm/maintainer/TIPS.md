# Email Compiler Maintainer Tips
- Treat output naming changes as breaking for downstream consumers.
- Validate watch mode cleanup/rebuild behavior on rapid edits.
- Keep asset precedence rules explicit in tests.
- Recheck image embedding behavior after pipeline refactors.
