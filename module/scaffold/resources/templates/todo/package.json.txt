{
  "author": {
    "email": "{{{author.email}}}",
    "name": "{{{author.name}}}"
  },
  "dependencies": {
    {{#dependencies}}
    "{{{.}}}": "^{{{frameworkVersion}}}",
    {{/dependencies}}
    "@travetto/cli": "^{{{frameworkVersion}}}"
  },
  "devDependencies": {
    {{#devDependencies}}
    "{{{.}}}": "^{{{frameworkVersion}}}",
    {{/devDependencies}}
    "@travetto/compiler": "^{{{frameworkVersion}}}",
    "@travetto/pack": "^{{{frameworkVersion}}}"
  },
  "description": "A Travetto-based project",
  "keywords": [
    {{#moduleNames}}
    "{{{.}}}",
    {{/moduleNames}}
    "travetto"
  ],
  "license": "MIT",
  "scripts": {
    "build": "npx trvc build",    
    {{#module_web}}"start": "npx trv web:http",{{/module_web}}
    {{#module_test}}"test": "npx trv test",{{/module_test}}
    {{#module_lint}}"lint": "npx trv lint",{{/module_lint}}
    "watch": "npx trvc watch"
  },
  "files": [
    "src",
    "resources"
  ],
  "name": "{{{name}}}",
  "version": "1.0.0"
}
