{
  "name": "@encore/auth",
  "version": "1.0.0",
  "description": "",
  "main": "src/lib/",
  "scripts": {
    "test": "mocha --require src/test/ --ui with-context src/test/**/*.ts",
    "test:single": "mocha --require src/test/ --ui with-context",
    "test:bamboo": "mocha -R json --require src/test/ --ui with-context src/test/**/*.ts > mocha.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@encore/express": "git+ssh://git@git.eaiti.com/encore/express.git",
    "passport": "^0.3.2",
    "@types/passport": "^0.2.30"
  }
}