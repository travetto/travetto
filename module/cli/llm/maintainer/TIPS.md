# CLI Maintainer Tips
- Treat command names and flag names as compatibility-sensitive.
- Add regression coverage when modifying option parsing semantics.
- Keep stderr/stdout behavior predictable for automation consumers.
