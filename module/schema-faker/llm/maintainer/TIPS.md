# Schema Faker Maintainer Tips
- Treat mapping edits as behavior changes, not refactors.
- Keep regression fixtures for frequently mapped field names.
- Document faker API version impacts on outputs.
- Watch for performance regressions in deep nested schemas.
