import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
    selector: 'app-landing',
    templateUrl: './landing.component.html.ts',
    styleUrls: ['./landing.component.scss.ts'],
    imports: [RouterLink]
})
export class LandingComponent { }
